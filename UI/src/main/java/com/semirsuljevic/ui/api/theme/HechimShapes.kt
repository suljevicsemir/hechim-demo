package com.semirsuljevic.ui.api.theme

import androidx.compose.foundation.shape.RoundedCornerShape

object HechimShapes {
    val smallRoundedCorner = RoundedCornerShape(HechimSizes.small)
    val mediumRoundedCorner = RoundedCornerShape(HechimSizes.medium)
    val largeRoundedCorner = RoundedCornerShape(HechimSizes.large)
    val xLargeRoundedCorner = RoundedCornerShape(HechimSizes.xLarge)
}
